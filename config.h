#pragma once

#include <string>

static const std::string DEVICE = "sysdefault:CARD=Q20";

static const std::size_t WIN_WIDTH = 100;
static const std::size_t WIN_HIGHT = 10;

static const std::uint16_t PORT = 80;

